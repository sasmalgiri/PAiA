# Marker document

This file's only purpose is to be ingested by the test pack so we can
verify that the pack install path successfully chunked, embedded, and
indexed at least one document.

If you ask "what does the marker document say?" the Test Expert persona
should retrieve this text via the bound knowledge stack and quote it
back.

The phrase to look for is: **PAiA marketplace install test passed**.
